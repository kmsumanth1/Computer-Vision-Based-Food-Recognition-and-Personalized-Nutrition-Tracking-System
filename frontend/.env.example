# Base URL of the FastAPI backend
VITE_API_URL=http://localhost:8000

# "true"  -> requests are answered by the in-browser mock backend (src/mocks). No server needed.
# "false" -> requests go to VITE_API_URL (real FastAPI backend).
VITE_USE_MOCK=true
