# Web
fastapi>=0.115,<1
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.1

# Database (MySQL)
sqlalchemy>=2.0.30,<2.1
pymysql>=1.1
cryptography>=42        # needed by PyMySQL for MySQL 8 auth (caching_sha2_password)
alembic>=1.13

# Timezones (Windows and slim containers have no timezone database of their own)
tzdata>=2024.1

# Auth
pyjwt>=2.8
argon2-cffi>=23.1

# Image handling + inference (runs the trained model exported as ONNX)
pillow>=10.3
numpy>=1.26
onnxruntime>=1.18

# HTTP client (optional Open Food Facts barcode lookup)
httpx>=0.27